node_modules/
lib/
coverage/
*.tsbuildinfo
.DS_Store
.claude/

# expo local state
.expo/

# local device bug reports, never part of the package
bugreport-*.zip

# local env (Expo reads .env.local; keys must never be committed)
.env*.local

# npm auth belongs in ~/.npmrc, never in the repo
.npmrc
