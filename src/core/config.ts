/**
 * Where the SDK talks to by default.
 *
 * Apps only need to supply an API key; the service URL is our infrastructure
 * detail, not theirs. Passing `apiUrl` explicitly still works and is how the
 * example app points at a local platform during development.
 */
export const DEFAULT_API_URL = 'https://support-platform-pearl.vercel.app';
